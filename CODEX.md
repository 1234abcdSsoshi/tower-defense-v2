# CODEX.md

時代戦線 序戦 ── Canvas2D の横画面タワーディフェンス。
TypeScript + Vite + Tauri。網につながずに完全に動く（絵も音も書体も同梱）。

Codex がこの repo で主に受け持つのは**素材を足すこと** ── キャラクターの立ち絵と BGM。
コードを触る場合の決まりは [CLAUDE.md](CLAUDE.md) と同じ。

リモート: https://github.com/1234abcdSsoshi/tower-defense-v2.git
パッケージ管理: **pnpm**（npm は使わない）

---

## 毎回やること

**作業を終えたら、必ずこの順で締めくくる。** 途中で赤が出たら直してからやり直す。

```bash
pnpm check                            # 1. 型・lint・マスタ検査・テスト
git add -A                            # 2. 変更をすべて載せる
git commit -m "<日本語で、何をしたか>"  # 3. 記録する
git push                              # 4. GitHub へ送る
```

一行でまとめるなら:

```bash
pnpm check && git add -A && git commit -m "メッセージ" && git push
```

### この順番を崩さない理由

- `pnpm check` を通していないものを push すると、CI が赤いまま main が残る。
  main は常に遊べる状態でなければならない
- **絵と音は `pnpm check` では検査されない。** 型もテストも通るのに
  盤面で豆腐（□）が出る、絵が宙に浮く、といった事故はここをすり抜ける。
  下の「置いたあと必ず見る」を必ずやること
- `git add -A` にしているのは、新しく作ったファイルの入れ忘れを防ぐため。
  素材を足したのにコミットし忘れると、**clone した人の手元だけ絵が出ない**
- push まで含めて 1 回の作業

### commit メッセージの書きかた

一行目に**何をしたか**を日本語で。理由が要るなら空行のあと本文に。

```
原始時代の立ち絵と畏の音を追加

妖怪5・退魔師3・人間3 の11体。既存82枚に合わせて
高さ256px・透過・右向き。畏の層は無音程・無拍で1本。
```

### push が弾かれたら

誰かが先に push している。**強制 push はしない。**

```bash
git pull --rebase origin main
pnpm check        # 取り込んだ結果でもう一度通す
git push
```

**この repo は複数の作業者が同時に触る。** 自分が作っていないファイルが
作業ツリーに現れることがある。他人の未コミットの変更を消さないこと。
自分が触ったファイルだけを `git add` するほうが安全な場面もある。

---

## 絵を足す

### 置き場と読み込み

`src/assets/units/` に置くだけでよい。**コードへの登録は要らない。**
`src/render/unitSprites.ts` が `import.meta.glob` で自動的に拾う。

### ファイル名が鍵

```
<id>-era<番号>-<英字の説明>.png

例) orochi-era0-yamata-no-orochi.png
    blade-era3-swordsman.png
```

- `<id>` は `master.json` の系譜 id と**一字一句同じ**。違うと読み込まれず、
  手続き描画のまま静かに落ちる（エラーは出ない）
- `<番号>` は `0=原始 1=古代 2=中世 3=近世 4=近代 5=現代`
- `<英字の説明>` は自由。読み込みには使わない
- 例外が二つ。`boss-era<N>-…` は時代の主、`yokai-era<N>-…` は妖の召喚
- `siegeh` だけコード側で `siegeH` に読み替えている

### 寸法

| 項目 | 決まり                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------- |
| 高さ | **256px ちょうど**。全 93 枚が例外なくこの高さ                                                          |
| 合計 | いま 8.5MB。ブラウザ版は初回にこれを丸ごと落とすので、増やしすぎない                                    |
| 幅   | 可変（実測 156〜569px、中央値 256px）                                                                   |
| 背景 | 完全透過。影・接地円は描かない（ゲーム側で足す）                                                        |
| 向き | **右向き**。敵側は `src/render/unit.ts` の `scale(-1, 1)` で反転する                                    |
| 接地 | 足の裏を下端に。宙に浮かせない                                                                          |
| 中央 | 体の中心を画像の中心に。武器が伸びても中心は体                                                          |
| 重さ | 1枚 **150KB 以内**を目安（実測は 51〜213KB、中央値 86KB）。網に繋がずに遊ぶ配布なので、大きい絵は詰める |

### 画風

既存 93 枚と並べて違和感が出ないことが最優先。
アニメ／漫画調のデジタルイラスト、黒の主線に強弱、セル塗り＋やわらかい
グラデーション、彩度は高めだが土っぽい。厚塗りにもドット絵にもしない。
小さく表示しても判別できるシルエットにする。

迷ったら `yokai-era2-oni.png` と `blade-era3-swordsman.png` を見ること。

---

## 音を足す

`src/assets/audio/` に `.ogg` で置き、`src/audio/index.ts` から
`import 名前 from "@/assets/audio/○○.ogg?url"` で読む（絵と違い自動では拾わない）。

いま入っているのは三系統。

| 系統   | 本数 | 中身                                                        |
| ------ | ---- | ----------------------------------------------------------- |
| 戦闘外 | 1    | `jidai-adventure.ogg`。拠点・編成・結果                     |
| 戦闘中 | 6    | `era0`〜`era5`。時代ごと                                    |
| 畏     | 1    | `awe-layer.ogg`。戦闘曲の上に重ね、畏の値で音量を上下させる |

### 畏の層に拍と音程を持たせない

重ねる相手（戦闘曲6本）が互いに揃っていないため。

- BPM が `72 / 82 / 112 / 104 / 114 / 128` と全部違う → **拍があると必ずずれる**
- 根音が D（4本）／E（1本）／F（1本）と割れている → **音程があると必ず濁る**

息、囁き、低い唸り、遠い余韻、逆再生の風 ── 輪郭を持たない音だけにする。

---

## 置いたあと必ず見る

型もテストも通るのに、絵と音の事故はすり抜ける。**目と耳で確かめること。**

```bash
pnpm dev        # http://127.0.0.1:5173
```

一戦して、次の四つを見る。

1. **絵が出ているか。** 名前を間違えると手続き描画のまま、エラーも出ずに進む
2. **足が地についているか。** 下端の余白がずれると宙に浮く
3. **豆腐（□）が出ていないか。** 下の `pnpm fonts` を参照
4. **音が鳴るか。** 畏を上げたときに層が乗るか

---

## 新しい名前を画面に出したら、書体を焼き直す

同梱書体は**使う字だけ**に絞ってある。キャラクター名を `master.json` に足すと、
その字が書体に無ければ画面で豆腐（□）になる。

```bash
node scripts/collect-glyphs.mjs                 # 使う字を集め直す
node scripts/build-fonts.mjs <原本のTTFの置き場>  # 焼き直す
```

**`pnpm fonts` は原本の置き場を渡せないので、そのままでは二段目で失敗する。**
原本は Google Fonts（OFL）の配布そのまま。repo には含めていないので、
無ければ次の6つを落としてから渡す。

```
ShipporiMinchoB1-SemiBold.ttf / ShipporiMinchoB1-ExtraBold.ttf
ZenKakuGothicNew-Medium.ttf / ZenKakuGothicNew-Bold.ttf / ZenKakuGothicNew-Black.ttf
IBMPlexMono-Medium.ttf
```

焼き直したか確かめるには、新しい名前の字が
`src/assets/fonts/ShipporiMinchoB1-SemiBold.woff2` の cmap に入っているかを見る。

---

## 絶対に壊してはいけないもの

**一戦の結果は、シードと入力ログだけで完全に再現できる。**
ゴースト・リプレイがこれに乗っている。

- 盤面に効く乱数は `G.rng()` だけ。演出の揺らぎは `vrng()`。
  **`Math.random()` を盤面に使うと、保存済みのゴーストが全部無効になる**
- シムは実時間を見ない（`DT = 1/60` の固定タイムステップ）
- シムは DOM を知らない。`src/sim/` から `src/ui/` を import しない

`tests/determinism.test.ts` が見張っている。ここが落ちたら手を止めること。

**手続き描画への落ちしろを外さない。** `unitSprites.ts` は読み込み前と失敗時に
手続き描画へ落ちるようにしてある。画像のデコードが間に合わないだけで盤面が
空になると、原因が最も追いにくい種類の不具合になる。

---

## 落とし穴

- **絵のファイル名を間違えても、エラーは出ない。** 静かに手続き描画のままになる。
  必ず開発サーバで目で見ること
- **`master.json` に系譜を足したら `id` と絵のファイル名を揃える。**
  先に絵を描いてから id を決めると、名前を変えるはめになる
- `master.json` を編集したら `pnpm validate:master`（`pnpm check` に含まれる）
- 時代を1つ増やすときは、時代数ぶんの配列**すべて**を伸ばす（検査が見張っている）
- `REC_V` を上げると保存済みのゴーストが捨てられる。
  盤面の挙動を変えたときだけ上げる（絵と音を足すだけなら上げない）
- `SAVE_V` を上げると全ユーザーの進行が消える。`loadSave()` に読み替えを足すこと
- `asset/`（原寸の素材）は Git で追跡していない。手元にしか無いので、
  消す前に控えを取ること。ゲームが読むのは `src/assets/` のほう
- **npm は使わない。** `package-lock.json` ができてロックが二重になる。
  依存を足すときは `pnpm add -D <名前>`、`npx` の代わりは `pnpm dlx`
